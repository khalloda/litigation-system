# Stage 1 review — 21 August 2026

VERDICT: fix these first

MUST FIX  (2 items)

- **The checks prove the number of mappings, but not that each mapping is the right one.** The database is correct today: I compared all 347 historical spellings with the reviewed SQL and compared all 20 crosswalk rules with their sources, and found no mismatch. But `npm run db:check` can still pass after an unchecked spelling is attached to the wrong person, because it checks the total, uniqueness, people's own-name aliases and two named hamza pairs—not all 347 spelling-to-person links. It can also pass if, for example, `دعاوى عمالية` is changed from `عمال` to the existing category `مدني`, because the crosswalk check only proves that the destination exists. In Stage 2, either fault would silently attach historical work to the wrong lawyer or classify matters incorrectly. Add a baseline pair-by-pair check, or a stable digest, for the 347 reviewed alias links and the 20 exact crosswalk links; it should allow deliberate future additions but refuse changes to the reviewed baseline. **Cost: about 2–4 hours, including negative tests.**

- **Two merged people now have two aliases marked as their primary name.** `أحمد عبد الله` has both `أحمد عبد الله` and `احمد عبدالله` marked primary; `أحمد فرحات` has both hamza spellings marked primary. Migration 0005 correctly asserted that nobody had two primaries, but migration 0006 moved the phantom people's primary aliases onto the surviving people and did not repeat that assertion. The live `npm run db:check` still reports all 17 checks passed. In real use, anything asking for “the primary spelling” can receive two answers. Demote the two moved spellings, add the missing live check, and preferably enforce one primary per person in PostgreSQL so this cannot recur. **Cost: about 30–60 minutes.**

SHOULD FIX  (1 item)

- **The team reviewer seed contradicts the project's own name-matching rule, and the team result is under-asserted.** The migration comment says team data is matched through `person_name_alias`, but both reviewer updates actually look up `ناجي رمضان` directly in `people.name_ar`. That is the exact matching method D5 and rule 15 prohibit. The assertions also check eight assigned people in total, not the exact four members in each team or either reviewer. The live data is currently right—both teams are reviewed by ناجي رمضان and the two four-person memberships match the SQL—but a miss or swap could otherwise remain green. Because the migration is already history, add a current postcondition that checks the two reviewers and exact memberships, and keep future name resolution through aliases. **Cost: about 45–90 minutes.**

MINOR  (1 item)

- `prisma/schema.prisma` still describes 146 lookup rows with an open branch question, 138 people and 339 aliases; the settled figures are 130, 135 and 347. This does not affect the database, but it can mislead the next developer or reviewer. **Cost: 10–15 minutes.**

WHAT LOOKS GOOD

- I compared the live database with the reviewed SQL field by field. All nine lookup lists match exactly: **130 of 130 rows**, with no missing, extra, altered, duplicated or unexpectedly inactive values. The 11 party roles are the exact D7 set.

- All **135 people** match the source after removing the three agreed phantom records. All **347 aliases** match the intended source-to-person links exactly. Every person is findable by their own name, and no spelling belongs to two people.

- The three merges lost no spellings. The survivors now hold 10 aliases for `أحمد عبد الله`, 6 for `أحمد فرحات` and 14 for `خالد عطيه`, including all four specifically reviewed fragments. The only merge defect is the primary flag described above.

- The branch resolution is correct. The 31-value list is now the exact 15 genuine sites; every one of the 16 removed values has its intended crosswalk row; all 20 crosswalk rules match the source; `المنطقة الحرة` remains a branch; and the old `جنح → الجنح → جنح` chain is collapsed directly to `client_branch جنح → matter_category جنح`. `آراء قانونية` correctly goes to `matter_type رأي قانوني`.

- D6 and D8 are followed: the team key exists only on a person, not a matter, and the editable lists are tables rather than PostgreSQL enums.

- The known `db:verify` issue is fixed correctly. The live history contains eight applied migrations, one cleanly rolled-back attempt and zero unfinished attempts. The failure predicate ignores the clean rollback but marks a row with neither `finished_at` nor `rolled_back_at` as unfinished, so a genuinely half-finished migration still fails.

- The `test:guard` deferral is correctly assessed. Its project-data inventory runs before cleanup or fixture creation, and the current lookup and people tables are outside its fixture allow-list, so rule 14 requires refusal. I ran no destructive test. The command could not reach that precheck in this Codex sandbox because the TypeScript runner failed while asking Windows for the current user; I did not bypass the restriction and do not claim that the 22 cases ran. Giving the suite its own throwaway database at task 2.3 remains the right resolution.

**Final verdict: fix the two MUST FIX items before closing Stage 1.**
