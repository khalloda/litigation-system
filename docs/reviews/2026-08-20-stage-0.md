# Stage 0 Review — 20 August 2026

VERDICT: fix these first

## MUST FIX (5 items)

- **Real client information is already committed.** [`src/strings.ts`](../../src/strings.ts#L110) explicitly labels a client name and three case numbers as real examples, and the homepage displays them. Other real examples appear in the documentation and stylesheet. Deleting them in a new commit is insufficient because they remain in Git history and have already reached `origin`. Real-world risk: the repository reveals a client relationship and identifiable case information. Replace all real examples with clearly fictional data, audit the repository, and clean the affected Git history. **Cost: about 2–4 hours**, assuming nobody else has cloned the repository.

- **`db:reset` can inspect one database and delete another.** It counts rows using `DATABASE_URL` at [`scripts/db-reset.ts`](../../scripts/db-reset.ts#L117), then deletes the fixed Docker volume at [line 191](../../scripts/db-reset.ts#L191). If the address points to a different empty local database, the check passes and the litigation volume is erased. It also counts only the `public` area, so it will ignore the separate staging area planned for migration. Real-world risk: extracted client data—or eventually the live database—could be erased without the expected warning. The guard must inspect the exact Docker database and every non-system area it is about to delete. Add deliberate tests for both bypasses. **Cost: 2–4 hours.**

- **The production protection fails open.** At [`scripts/db-reset.ts`](../../scripts/db-reset.ts#L68), an unset, misspelled, or differently capitalised `APP_ENV` is accepted as development. The live database is also on `localhost`, so forgetting one line in the server’s `.env` removes the supposedly unbreakable production protection. Unknown settings must refuse; only an exact, explicit development setting should permit reset. **Cost: 30–60 minutes including tests.**

- **The `.gitignore` does not cover several ordinary data formats.** It correctly blocks `.accdb`, `.csv`, `.xlsx`, and PDF, but `.xlsm`, `.xlsb`, `.ods`, `.sqlite`, `.sqlite3`, `.db`, ZIP and 7-Zip files remain committable. These can all carry client exports or complete databases. Add the missing patterns and an automated check using representative dangerous filenames. **Cost: 30–60 minutes.**

- **The build plan contradicts the settled logo decision.** [`TASKS.md`](../../TASKS.md#L108) instructs Claude to upload the 54 client logos to “object storage.” D15 requires a folder on the firm’s server. If followed, client logos could leave the chosen storage and backup arrangement. Change task 2.11 to the D15 server-folder location. **Cost: 5 minutes.**

## SHOULD FIX (3 items)

- **The RTL checker catches common mistakes, but not every violation it claims to catch.** It ignores styles written directly inside a React component, misses forms such as `.box { left: 0; }`, directional four-value shorthand, and checks only Arabic hardcoded text. The current page consequently contains visible English palette labels outside `src/strings.ts`. Add deliberately broken test fixtures covering these cases and enforce all visible interface text, not only Arabic. **Cost: 1–2 hours.**

- **The Access extraction can finish successfully after losing complex data.** [`scripts/01_extract_access.ps1`](../../scripts/01_extract_access.ps1#L328) catches attachment or multi-value read failures, records a warning, and continues. Its final gate only warns about zero rather than automatically requiring exactly 54 logos and 288 matter references. It correctly uses the Access object model, but it must stop on any mismatch before Stage 2 proceeds. **Cost: 30–60 minutes.**

- **`db:verify` gives a database error on a fresh, unmigrated database.** The query refers directly to `_prisma_migrations` even while trying to handle that table being absent. This does not endanger data, but the promised friendly “run the migration” guidance will not appear during a clean setup. **Cost: about 15 minutes.**

## MINOR (2 items)

- [`README.md`](../../README.md#L5) still says “no code written yet,” although Stage 0 is complete. **Cost: 2 minutes.**

- Several references incorrectly say court/circuit separation or teams are decision D18, including [`docs/DATA-MODEL.md`](../DATA-MODEL.md#L98). D18 is actually the parameterised client report. The intended design appears sound; the references are wrong. **Cost: 10 minutes.**

## WHAT LOOKS GOOD

- **The Arabic database setup is sound.** The running database passed all checks: PostgreSQL 17.11, UTF-8, ICU provider, `ar-EG`, the named Arabic collation, all three extensions, and Arabic sorting. Because the named collation is inside the Prisma migration, it will be recreated after a correctly targeted reset.

- The current interface genuinely has `<html lang="ar" dir="rtl">`, locally bundled Noto Naskh Arabic fonts, logical CSS properties, and multi-line case-number handling.

- The reset guard does refuse when it cannot connect, rejects a clearly non-local address, and reports populated public tables accurately. The problems are the gaps above, not the entire design.

- Git history contains no committed Access, CSV, Excel, PDF, database dump, private key, or real `.env` file. The confidentiality failure is the real information embedded in ordinary source and documentation.

- The production build succeeds. Type checking, linting, formatting, and the current RTL check pass. The Prisma application check passed 5/5 and the database check passed 10/10.

- I made no code changes or commits during the review; the working tree was clean when the review concluded.

**Final verdict: fix the five MUST FIX items before treating Stage 0 as safe to continue.**
