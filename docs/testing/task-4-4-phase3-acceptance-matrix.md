# Task 4.4 Phase 3 acceptance matrix

Executed 15 September 2026. Base:
`f05787471812c8fb2bd03d31bc44614dad81a8d2`. D63 was explicitly owner-adopted.
**Implementation checks passed; candidate pending independent review.**
This is not owner acceptance or overall Task 4.4 completion.

Evidence root: `D:\Projects\LitigationData\task44-phase3-20260915`.
Paths below are relative to that root (under `evidence/` in the review ZIP).
Source paths are repository-relative. Complete exact files and hashes are in the
external manifest and `source-bindings.json`; the committed report does not try
to contain its own final commit hash.

| Clause | Implementation | Executed evidence and result |
|---|---|---|
| D63.1: current authority at every layer; all-role reads | Four literal guarded pages and `lifecycle-actions.ts`; `src/lib/admin-lifecycle.ts`; migration69 state/save gateways; route inventory | PASS: `final-attempt12/lifecycle-results.json` service/state/gateway refusals for all four operations and other three roles, account disable/reset/demotion/version/expiry and different actor; `browser-attempt13/browser-results.json` four readers/pages; `direct-action-refusals.json` all 12 captured action refusals matched to actual server authorization digests. |
| D63.2: independent flags, all false initially, no cascades | Two default-false columns, narrow transition and guard, complete aggregate snapshots | PASS: `browser-attempt13/upgrade.json` old123 tables/48 full sequences; `final-attempt12/lifecycle-results.json` imported/native work/step archive/restore; restore work retains independently archived child and sibling/work/matter/client states. |
| D63.3: parent prerequisites and ordinary edits | Matter/work/step locks and guards; D62 save overlay; management-page archived-self/parent labels | PASS: `final-attempt12/lifecycle-results.json` NULL matter, archived client independence, archived matter four-operation refusals, archived work/step ordinary-write refusals, real changed work edit retaining archived child, foreign-parent step refusal; browser stale editor retains text. Detached current steps remain prohibited by existing permanent constraints/checks; quarantine untouched. |
| D63.4: filters, complete history, context and clamping | `admin-work-query.ts`, list/detail pages, `adminDetailHref`, matter link uses archive=all | PASS: independent complete paginated current/archived/all IDs/counts for four roles and four filter intersections in final12; malformed/duplicate enum/ID/query/return controls; browser13 round trips preserve q/status/matter/fromMatter/page/stepArchive/stepPage, direct archived reads, and last-visible-step-page clamp. Six current/archived/all screenshots plus clamp screenshot retained. |
| D63.5: content/source/order | Immutable original columns, lifecycle guard, shared D62 ordering; existing separately labelled source fields | PASS: old-column projections and original48 sequences preserved; final12 imported/native transitions retain data/order, append after archived maximum and duplicate native-order rejection; ID/D62 retained controls cover NULL/empty/multiline/inactive/unresolved references. Browser13 current cleared assignee and separately retained source, multiline and full-history views. No hidden nextAppointment/editor expansion. |
| D63.6: reports and relationships | Report services unchanged; historical counts remain independent of flags | PASS: `final-attempt12/report-visibility.json` six nonempty full before/after datasets (82/752/2/12848/3694/1140 rows), financial values included; matter2418 keeps23 works/55 steps. Existing historical-source report criteria remain unchanged; no new native-report feature claimed. |
| D63.7: deliberate Arabic confirmation | `admin-lifecycle-page.tsx`, `admin-lifecycle.tsx`, centralized strings; strict facts/version/subject validation | PASS: browser13 four confirmation/Cancel/Escape/focus/changed flows, exact subject/counts/date/content and filter round trips; stale message and deliberate reload; full-state read/Cancel equality; backend forged facts and stale refusals. Four standalone confirmation screenshots. |
| D63.8: shared version/history/audit | Migration69 replacement `admin_edit_current_valid`, immutable boundary, one shared receipt table and aggregate version | PASS: final12 correct subject/version/actor/history/receipt/semantic assertions; 13 targeted corruptions rejected and rolled back with full fixture equality. Old native68 work/step and imported edit receipts preserved in browser13; four post69 lifecycle transitions advance upgraded aggregate exactly four, old receipt replays still write nothing. |
| D63.9: no-op/retry/concurrency | Shared mutex/token namespace; authority before receipt; parent/work/step locks; strict fresh version/facts | PASS: final12 imported/native work and step fresh no-ops and exact retries, shared-edit token crossing/changed action/payload/subject/parent/actor refusals, archived-parent receipt replay; seven observed mutex overlaps plus two observed matter-row overlaps with lock barriers. No timing-only success claim. Browser13 four old receipt replays produce no table/sequence delta and show fresh current state. |
| D63.10: upgrade and permanent protection | Forward69 only; exact-checkpoint/schema/audit inventories; historical-version interpretation | PASS: full-state15/135→137 in browser13; empty canonical15/117→119 in canonical10; real edited/native68 boundary and old receipts; late injected69 failure rolls back full data/catalog/48 sequence state; final source checkpoints and rejecting guards retained. Original71 frozen migration/governance files, D1–D62 prefix and86 checkbox lines verified in `frozen-final.json`. |
| Production browser, Arabic/RTL and access | Private mirror/generated Prisma/build/runtime/port/profile; established logical styles | PASS: browser13 fresh build, four readers, three D62 writers and Lawyer refusals, lifecycle flows, 320px, actual200% browser zoom, accessible control/focus assertions and visual screenshot inspection. `production-build-identity.json`, `browser-runtime.json`, captions/index and `visual-review.json`. No OS screen-reader speech and no full accessibility-conformance claim. |
| Required static and permission gates | Exact source/route/audit inventories; existing rejecting test fixtures | PASS: all npm check gates in `check-final.log` plus successful `check-encoding-final.log` after adding the required BOM to an uncommitted PowerShell helper; `static-closure.json` records the composed result without calling the original exit-1 command a pass. `permissions.log`448 static policy decisions plus structural/negative assertions; whitespace/frozen checks. Unchanged full user-management database fixtures are reused from accepted evidence; lifecycle fresh authority checks are new. |
| Owner and artifact preservation; closure | Forced-read-only snapshots; full file hashes; fresh process observation; owned-only cleanup | PASS: `owner-preservation.json`, `accepted-process-final.json`, `cleanup-final.json`, input verification and complete package verification. Actual owner68/acceptedPhase2 build unchanged. Protected file scope and snapshot exclusions are explicit in receipts/report. One local child of f057874, no push/activation, stop independent review. |

## New, reused and failed evidence

Final backend is `final-attempt12`; final complete browser/edited-upgrade run is
`browser-attempt13`; complete canonical run is `canonical-attempt10`. The first
two share exact application/migration/build-source inputs; only a browser wait
assertion changed. Canonical replay dependencies are unchanged; later runner
branches affect edited/browser proof only.

Within-phase reuse: `full-attempt7/id-candidates.json`, `green-results.json`,
`mutation-results.json` and `d62-adversarial.json` preserve the 12-candidate/four-role
ID proof, seven editing groups and 12 adversarial cases. Application, SQL and
behavior-proof inputs are identical. The later lifecycle structural checker was
strengthened and passed the final137 checks. Run7 later failed an unrelated
lifecycle harness case and is not presented as a whole-run PASS.

`evidence-ledger.json` identifies all 13 attempts, their recovery and exact retained
inputs. Attempts1–3 lack a full executed-source manifest and per-attempt owner-after
receipt; available logs/identities are preserved, and they supply no successful
candidate proof. The final overall owner equality/resource inventory covers the
phase. Attempt8's pre-import browser-source drift is separately disclosed.
The final run remained frozen. Git clean-filter bindings distinguish working
LF/CRLF bytes from committed blobs; normalized equality alone is not used as
exact artifact evidence.

See the [implementation report](../task-reports/2026-09-15-task-4-4-phase-3-archive-restore.md)
for commands, scope, failed-attempt explanations and the independent-review stop.


## Owner acceptance and local activation — 15 September 2026

The owner accepted Phase 3 at `2966570c368ef484b4d32c77d5bd4c971c19ff4f` and its
[independent PASS review](../reviews/2026-09-15-task-4-4-phase-3-independent-review.md).
The combined mandate authorized protected backup/restore/rehearsal, exact actual
migration 69, accepted-app activation and ordinary publication of one eight-document
child over the accepted source. Overall Task 4.4 remains unchecked.

Actual migration 69 is verified: 137 historical invariants and 15 setup checks;
all 123 old-table projections, original account/session/credential state and all
48 full sequence vectors remain exact. Two false archive flags, the exact source-derived
private boundary, reviewed named catalog changes, two audit registrations and
the new ledger entry are the only permitted delta. The same new backup passed
135 pre-69 and 137 post-69 invariants plus 15 setup checks on the owned restore.
Four independent lifecycle transitions and all four confirmation/Cancel flows
passed focused authenticated checks on that restore using the final accepted build.

Build `LyN77RMhXZbOvG44OcUhg` runs on actual `http://127.0.0.1:3000`.
Actual anonymous RTL/assets/protected-route/mobile smoke passed. Actual authenticated
observation is pending: interactive browser tools failed kernel initialization,
including a reset/retry; no owner cookie was fabricated, password reset or business
record changed. The accepted exhaustive implementation evidence is reused with
its original limits, not represented as a new full run. No screen-reader claim.

See the [acceptance/activation report](../task-reports/2026-09-15-task-4-4-phase-3-acceptance-activation.md)
and external activation/publication/delivery receipts. Stop for independent review;
all existing checkbox lines, D1–D63 and governance remain unchanged.
