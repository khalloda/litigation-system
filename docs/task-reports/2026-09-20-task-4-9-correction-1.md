# Task 4.9 correction 1 — implementation review candidate

20 September 2026. **Not accepted, not activated, NOT PUSHED.** One corrective
local child of `fe4452d90c9b046097effcf874540c1737e8d002`; published base remains
`ba360643937fd0af9d11b12b7f18f8ddec093aac`. Final child/tree, exact corrective and
cumulative statistics are bound by the external receipt and raw Git objects,
not a self-referential hash in this report. The first candidate/report/evidence
are preserved. All 86 checkbox lines, D1–D67, governance files and migrations
1–72 remain unchanged.

## Authority, configuration and scope

The owner adopted the correction prompt and unchanged
[independent findings](../reviews/2026-09-20-task49-independent-implementation-review.md),
retaining the complete original Task 4.9 viewer and export mandate. Both prompts,
the actual authorization and input companions are in the package. Same Local
Windows Desktop task at `D:\Projects\litigation-system`; no subagents/worktree.
Requested GPT-5.6 Sol / High selector is not observable, so no model-selection
claim is made. Expected usage moderate; verified original evidence, existing
locked dependencies and Chromium were reused without a paid service, global
installation, dependency upgrade or owner credential rotation.

Only disposable copies received migration/provisioning, fixture passwords,
mutations, hostile probes, logins and exports. Actual owner migration, capability,
activation/restart, acceptance closure, push and Stage 5 remain unauthorized.
Task 4.8 was not repeated. Next step is independent correction review.

## R1 — complete transaction-boundary enforcement

The restricted runtime generic writer reproduced the reported orphan completion
under original 73. The diagnostic first rolled back with full-state equality;
a separate COMMIT confirmed the defect. That poisoned cluster was wholly
discarded, never reused as a passing baseline or repaired by deleting history.

Revised unaccepted migration 73 adds `audit_export_event_consistency`, a deferred
AFTER INSERT constraint trigger. Its exact private helper checks reserved
completion and grant/revoke facts against receipts, capability chain and current
state. Existing private-side guards remain. Literal `starts_with(...,
'audit_history:')` replaces LIKE's wildcard interpretation. Ordinary resource
auditing remains supported; current authorization, idempotency, generation-before-
audit and audit-before-release remain unchanged. The exact permanent trigger,
function, privilege and source inventories are extended without weakening guards.

Revised migration SHA-256:
`91dc1189cbde3d57cc4d7c99755498f16cffe69373add6d1e3c15b3b5e6e7431`.
Original committed/executed bytes remain retained. Revised rehearsal starts at
fresh 72; there is no checksum repair, migration 74 or actual owner migration.

## R2 — lossless values and distinct display

Before/after XLSX evidence now encodes a documented typed JSON envelope:
`{present:false}` or `{present:true,kind,text}`. Exact text is not translated or
parsed as a JavaScript number. UTF-8/Base64 chunks are ordered and bounded even
for three-byte Unicode; readable cells are separate and literal. Metadata rows
are explicitly described as display-text envelopes, not raw database records.

Nonempty strings display a text prefix and guillemets. Thus literal strings
matching empty/null/false/absent/redaction/truncation labels remain visibly text.
Whitespace-only strings and forbidden XML controls have escaped readable forms;
ordinary multiline Arabic/mixed text stays multiline. Raw evidence remains exact.
Formula/HTML/URI safety, bundled fonts, external-fetch blocking, result bounds,
full selected scope and current authority checks are preserved.

`values-02` specifies 23 independent typed cases. Python standard-library ZIP/XML/
Base64 decoding of the actual workbook equals every original input, with ordered
chunks, RTL sheets and no formula cells. Final PDF page images are pixel-identical
to the inspected five-page synthetic rendering; pages 2–5 were visually reviewed.
`browser-03` exercises the production viewer with a clearly labelled synthetic
response solely for renderer collisions; live SQL/HTTP cases separately prove
authentication/scope. Synthetic values are not invented historical events.

## R3 — explicit complete browser coverage

The test declares 23 required entity families and fails if any are absent. It
selects verified child/parent pairs rather than the first parent and incidental
buttons. Final result: **29 entry openings, 26 distinct surfaces, 23 families**.
All seven missed families are covered, including actual allocation IDs, contact
parent and own-detail routes, both separate fee-letter directions and retained
fee relationships. Other retired-family populations are explicitly queried and
absent, not claimed tested. Global-only attendance/lookups remain explicit; no
new standalone workflow is invented.

Every selected scope compares actual event IDs to independent raw-event and
retained-association joins, including honest empty histories. Evidence records
route/table/child ID, expected/actual IDs, open drawer, initial Close focus,
14 keyboard steps, Escape and return to the exact trigger. Successful focus
traces and screenshots are retained. Shared Clear/drafts, Back/Forward, stale
responses, role/method denial, 320px reflow and true 200% browser zoom pass.
Viewing/refusals preserve complete fixture state after login setup.

The requested form-labelling, error-prevention-recovery, focus-attention-design
and accessibility-testing-strategy skills informed persistent labels, explicit
state wording, focus recovery and the automated/manual evidence boundary.
Repository exact-value rules override generic normalization suggestions. No
screen-reader speech test or accessibility-conformance claim.

## Fresh verification map

All paths below are relative to `test-results/task49-correction1-20260920` and
packaged under `evidence/`. Every wrapper result binds exact executed shareable
source; timestamps, commands/stdout/stderr and meaningful failures are retained.

| Requirement | Fresh evidence and result |
| --- | --- |
| Handoff/original identity | `commands/input-verification*`, `commands/verify-original*`; exact 871-member handoff and original 3,289-member five-file delivery |
| Original R1 | `original-r1-probe/proof/reproduction.json`: REPRODUCED, not PASS; diagnostic rollback and separate orphan COMMIT |
| Full-data revised upgrade | `migration-01`, `upgrade-01`, `upgrade-comparison.json`: 138→141 tables; all 48 full sequences exact; prior business/history rows preserved; one grant/ledger/counter delta |
| R1 invariant | `invariant-02/proof`: generic runtime immediate/COMMIT refusal; literal namespace; privileged orphan grant/revoke, current-state, chain and receipt negatives; missing/disabled trigger negatives; full rollback exact |
| Reader | `core-01/proof`: 905 events, 47 groups, 7,816 typed scalars; filters/pagination/current authority and exact read/refusal state |
| Boundaries | `boundary-01/proof`: 35 scope oracles, 11 corruption cases, 87 same-microsecond edge events and exact rollback |
| Relationships | `relationships-01/proof`: genuine covered add/retire/restore and separate reference set/replace/clear; original evidence unchanged |
| Export authority/safety | `exports-01/proof/result.json`: eight result groups pass; role/forged payload/origin/abort/generation/audit failures, rename/revocation, full global/context/empty exports, 200/409 same-operation with one event |
| Typed evidence | `values-02/proof/independent-xlsx-decoding.json`, PDF page renders and `visual-inspection.md`: 23 exact typed cases |
| Browser | `browser-03/proof`: final build `57OStUe4LigLu2vfgBTU-`; complete mapping, interactions, screenshots, focus and actual downloads; task process SIGTERM exit recorded |
| Permissions | `permissions-01`: original 448 unchanged +32 =480; wrapper/static/account-state negatives |
| Source/build | `check-02`: full check including 150 guarded entries, 67 audit bypass negatives, 8 account negatives; `build-01`: production build passed |
| Historical DB | `db-final-01`: all 148 permanent checks at corrected73 |
| Canonical DB | `canonical-01`: isolated clean replay through73, all130 permanent checks; exact final output included |
| Final source/document gate | `check-final`: current complete check; source-only documentation updates do not change the tested build's runtime closure |
| Preservation/cleanup | `preservation-comparison.json`, `resource-reconciliation.json`, final owner/files/runtime observations after all tests and cleanup |

The original check-60 foundation result is reused only as historical foundation
proof, never renamed migration73 proof. Original exhaustive authentication and
account lifecycle results are reused because their service/session/password/
management source closures are byte-identical; fresh core/permissions/export/
browser/static checks cover the affected integration. `reuse-bindings.json`
lists exact body identities. The reader SQL was unchanged; outer wrapper plans
are not newly presented as inner query-plan evidence.

## Preserved state and execution errata

The fresh protected full-data dump is 22,530,264 bytes, SHA-256
`a40812a855208ca2ea75c9d7f4fd905eff244049422d82a0d608148daad3854c`.
It and generated fixture secrets remain in the restricted task-private directory,
not this Git commit or review ZIP. All 54 logos were copied and hash-verified in
task storage; originals were not changed. Original recovery/evidence remains.

Before/final owner observations compare 138 full tables, 48 complete sequence
vectors, 15 catalog categories, 880 individual prior audit rows, 73 ledger rows,
account/person/credential/session state, counters and retained history. File proof
covers 160,274 prior files, ACL roots and 21 junctions, including accepted
artifacts, old evidence, root dependencies/build, private recovery and configuration.
Final receipts supply exact comparison results and timestamps after all tests.
Owner PID52576/build `l02XdOB10LpyGNufC8_Fs`/loopback3000 are freshly observed,
not inferred from an old PID. No process restart or owner write was authorized.

`attempt-notes.md` discloses the sandbox launcher failure, rejected array indexing,
superseded browser screenshots and null-exit-code reporting issue. Original failed
attempts/missing intermediate bodies are not erased or fabricated. Existing logo
tracing build warnings are retained without unrelated changes. No safety override.
Fixture lifecycle cleanup is serial and exact resource reconciliation is supplied.

A final private-root ACL scan found Chromium-added capability permissions on
nine newly created task-profile directories. After all browsers closed, only
those extra entries were retired, retaining every file and the before/after ACL
evidence. A full scan then passed for 999 private entries with only owner, SYSTEM
and Administrators access. No original owner/evidence ACL changed. Fresh
`owner-after-final` and `runtime-after-final` observations follow that last cleanup;
the first after-observations are preserved too. No further runtime test or cleanup
is performed after these final observations.

## Delivery and stop

The five-file correction delivery contains full shareable base/reviewed/corrected
source, three raw commits and full trees, both full-index binary-safe patches and
bidirectional reconstruction, original/corrective authorities, fresh/reused proof,
complete inventories, standalone verifier, result and non-circular receipt.
The two unchanged credential-bearing source files have identity-only coverage.
Raw dumps, credentials/cookies, full privileged global exports, logo binaries and
build/dependency binaries are not distributed. Safe synthetic outputs are included.

The old report's universal H08 losslessness, H14 commit enforcement and implied
complete H20 coverage are expressly superseded by this correction; unchanged
claims are reused only with source/risk bindings. Verification is implementation
evidence, not independent acceptance.

**NOT PUSHED; STOPPED FOR INDEPENDENT IMPLEMENTATION REVIEW.**
