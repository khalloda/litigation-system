# Task 4.9 acceptance migration activation and publication

Operational work: 20–21 September 2026 UTC. The filename follows the adopted
20 September handoff; the genuine download recovery completed on 21 September.

## Outcome and authority

Khaled accepted independently reviewed corrected implementation
`64b5a191e7b9e0d2776c53df69ee6350ca154a67`, tree
`ab7f4831ee95249b92d938346c83f06f9c6f952a`, sole parent
`fe4452d90c9b046097effcf874540c1737e8d002`. The
[unchanged independent PASS](../reviews/2026-09-20-task49-correction1-independent-review.md)
closes R1–R3. His direct message adopted the verified operational handoff and
authorized exact migration 73, the initial verified capability, full activation,
bounded actual exports, one documentation child and non-forced publication of
`ba360643 → fe4452d → 64b5a19 → that child` to the existing GitHub main.
The attachment did not independently supply authority. Work remained in this
same local Windows task, without subagents.

The full Administrator viewer and separately persisted-capability XLSX/PDF
feature are accepted and active. Only Task 4.9 is newly checked. The other 85
checkbox lines, Task 4.8 closure and the accepted implementation remain intact.
Stage 5, migration 74, further grants, credential rotation, governance changes
and Ubuntu deployment were not undertaken. Independent operational review is
the next stop. This report cannot contain its own future commit identity or a
future push result; exact commit/tree/statistics and publication observations
belong to the external review receipt.

## Verified inputs and source

The 8,528,375-byte operational handoff ZIP has SHA-256
`06cc1f958c44e36a0b5355b288816462c546e0040a595d447e6b1f12d7fd38dd`.
All 874 safe unique regular members passed sizes, CRC and SHA-256 against the
separate unchanged manifest; the standalone prompt matched byte for byte.
The original correction delivery was located and freshly verified: 22,969
members, ZIP SHA-256 `a5f2f8f663c7f58a4a0c0dac5a7c520133e40df96e6892f84b4a6e5fa36d368e`.
All 826 accepted source identities were bound to the stable build. The two
unchanged credential-bearing tracked bodies remain identity-only in shared
evidence and are checked privately. Application, tests, schema, dependencies,
governance and migrations 1–73 were not edited.

## Recovery and isolated rehearsal

The fresh owner baseline covered every live column of all 138 non-system tables,
complete row/column digests, all 48 sequence definitions and last_value/log_cnt/
is_called vectors, exact named catalogs/grants/roles/configuration, and every
prior audit/ledger ID and full-row digest. The initial state was 72 completed
migrations, 73 ledger rows including the historical rollback and 880 audit rows.
The full unique recipient predicate confirmed existing usable Administrator
account 2, person 139, human actor 1002. Canonical account 3 was not substituted.

After building, only the positively identified old app PID 52576 was stopped.
The database stayed running. Complete quiescent equality established a recovery
boundary with no unaccounted writer. Protected recovery is retained at
`D:\Projects\LitigationData\DB-Backup\migration63\pre-migration73-20260920T205957Z-4736562a`.
The custom dump is 22,530,264 bytes, SHA-256
`80b8a342e555f1d3728586c357a4b3a813ce12cfceffb918dddca50738f86f61`.
The package includes roles/ownership/grants/configuration recovery, all 54 logos,
ACL records and a non-circular inventory. Secret-bearing material is restricted
to the existing owner/SYSTEM/Administrators boundary. This uses the approved
local-development exception; laptop/disk loss is not covered.

That exact dump was restored to the labelled owned PostgreSQL 17 fixture,
cluster `7687730906756177958`, port 39133, distinct from owner cluster
`7676117521894273062`. Complete pristine restore comparison passed with only
documented observed restore representation differences, not exceptions reused
for owner equality. Guarded `npm run db:migrate:deploy` applied only exact 73;
the fresh 148 historical and 15 setup checks passed before fixture login/export.
Fixture credentials/accounts and mutation tests stayed on the isolated cluster.

## Actual migration and capability

Immediately before actual deployment, the complete owner state equalled the
protected recovery point; only 73 was pending, no other writer was observed,
and the full recipient/source/SQL bindings were verified. The exact SQL is
29,961 bytes, SHA-256
`91dc1189cbde3d57cc4d7c99755498f16cffe69373add6d1e3c15b3b5e6e7431`.
The established guarded command completed once without resolve/reset/seed,
checksum repair, manual SQL substitution or a second grant.

Actual 138→141 tables added only the three reviewed private capability/change/
receipt tables. All 135 unrelated old table bodies, prior 880 audit rows, prior
73 ledger rows and all 48 complete sequence vectors remained exact. The only
new migration audit event was 881, successful audit_export_granted by existing
actor 1 system_migration, targeting account 2 / actor 1002, false→true, with the
reviewed maintenance context/reason. One enabled capability and matching
immutable change refer to that event; receipts were initially zero. Counter
880→881 and one successful exact-checksum ledger row are the other data changes.
There are 73 completed migrations and 74 total ledger rows.

The complete named catalog delta matches the reviewed correction reference:
eight relation entries, 24 relation-grant entries, 19 columns, 14 functions,
ten triggers and five indexes; the old action-shape constraint is replaced,
with twenty constraint entries added overall. No wildcard-prefix allowance
substitutes for exact definition/grant comparison. Event-side deferred R1
enforcement is active. Actual 148 historical / 15 setup checks and permanent
capability/receipt invariants passed. All other accounts remain ungranted.

## Stable activation and evidence coverage

The accepted artifact is
`D:\Projects\LitigationData\accepted-task49-64b5a19-20260920T205957Z-4736562a`.
Build `lrLMgtVrMuIaje6eDCMFe` runs as PID 58056, started
20 September 21:35:10.820414 UTC, on `127.0.0.1:3000`. The owner container and
cluster were not restarted. Existing credentials, restricted litigation_runtime
connection and logo semantics remain unchanged; maintenance credentials are
absent from the web process environment. The launcher resolves the existing
Chromium executable through the permitted non-secret AUDIT_PDF_CHROMIUM setting.
All three bundled Arabic font files and logo were verified. Its 1,452 inventoried
build/source output files were rehashed after use; all 826 source bindings passed.

Fresh same-artifact isolation passed core/reader checks, four-role access,
ordinary Administrator export refusal, both actual formats and exact per-export
state deltas. Browser proof covers 23 families, 26 surfaces and 29 openings,
four nonempty and 25 honest empties, 34 axe scans without violations, narrow
viewport, zoom and exact focus recovery. Independent isolated XLSX decoding
checked 160 envelopes; actual Chromium PDF pages were visually inspected.

Accepted unchanged proof is transparently reused: 480 permission decisions,
23 typed corner cases, 130 canonical checks, reader/relationship/invariant and
race/commit-fault coverage. Four original auth/account/foundation results remain
reused, with the original foundation checkpoint 60 limitation. Fresh operational
checks do not relabel that foundation as rerun at 73. The
[matrix supplement](../testing/task-4-9-acceptance-matrix.md) preserves all required
families and the exact preceding 12,021-byte matrix.

## Actual owner reads and bounded export recovery

Anonymous health/direct-route checks passed. A genuine existing owner session
provided global and account-drawer access, truthful empty client/contact history,
parent/child navigation, actor/date/action/field/value filters, normalized Arabic
and Western search, Clear with unsent drafts, Back/Forward, Tab/Escape and exact
trigger focus. Full database equality across navigation passed. Neither tool nor
fixture sessions were injected, and no tokens were captured/replayed. The later
regular-browser ready checkpoint also had no login bookkeeping changes.

All actual exports selected the one existing account-2 grant event 881, q=true,
action=audit_export_granted, one group and one event. The windows were separate:

| Event | Format and outcome | Watermark | Artifact SHA-256 |
| --- | --- | --- | --- |
| 882 | Initial server completion; saved file unavailable | 881 | `681381232612a7077b81a23db1909ca76b303bd3001e2552c5e3ac3592b3c059` |
| 883 | Initial PDF server completion; saved file unavailable | 881 | `69067b2a3e0ee53aac39d75cf2d9c458e70ad575383aa96ee0c7a93f6ead9c81` |
| 884 | Genuine owner-assisted XLSX recovery, 10,828 bytes | 883 | `71cdcca555109510df09a517097efbd3fb6a36b31a45d0d097f473a763bef027` |
| 885 | Genuine owner-assisted PDF recovery, 334,722 bytes | 884 | `621144dd8c5de1f4793ec4a8c5623dd11ac63d326cef1c81ec5a633a02d1a911` |

The in-app browser download-event wait timed out. Its server outcome was checked
before any recovery: one completion and matching receipt, not a presumed failed
export. With no recoverable saved copies exposed, the owner used the genuine
regular-browser session for one bounded replacement per format. Original events
were never deleted or rewritten. Each of all four windows has exactly one
export_completed event, one account-2 operation/request/artifact-bound receipt,
counter+1, and complete equality of all unrelated rows, prior audit/ledger,
capability/change state, all 48 sequence vectors and catalogs.

Downloaded recovery bytes exactly match their own immutable server receipts.
Independent ZIP/XML/Base64/JSON inspection confirms two RTL worksheets, no
formulas, 22 typed envelopes, exact boolean false/true grant values, the selected
event and truthful metadata. Both PDF pages were rasterized and visually checked:
connected Arabic, correct logo/fonts, scope/filter/count/watermark/generated-at,
continued recorded evidence and no clipping/overlap. Full privileged export
bodies, decoded raw values and PDF page images remain protected locally; only
necessary sanitized inspection evidence is shared. Initial server completions
are not misrepresented as independently verified client-file receipts.

Total owner audit rows/counter are 885: original 880 + one grant + four genuine
completions. There were no login, business, account, credential or other writes.

## Preservation operational deviations and finalization

The frozen protected set is 191,406 files / 8,797,145,196 bytes, 100 recorded
root/credential ACL observations and 29 junctions. Complete byte enumerations
are dated non-atomic scans, not per-descendant baseline ACL certification.
Earlier accepted artifacts, 54 logos, recovery and all prior evidence remain.
Only identified runtime-log changes may be accepted with original-prefix proof;
new recovery files and task-owned additions are enumerated separately.

One task-specific 1,156-byte Jiti cache was generated through the borrowed
dependency junction. The strict additions gate detected it; its original bytes,
hash and ACL were retained privately and only that new file was removed. A new
full comparison then passed all 191,406 old files and allowed only 70 new
protected recovery files. No old dependency or evidence file was removed.
The owned disposable cluster/network/volume and browser processes were cleaned
through scoped established helpers. Nine temporary Chromium capability ACLs on
closed task-created profile directories were retired; private descendant ACL
observations are retained separately. No blanket deletion or safety override.

Meaningful failed attempts remain in the external evidence: binary/CRLF
source-binding harness correction, wrong dependency inventory root, serialized
Date comparison, guessed old receipt filename, stopped sequential inventory,
stale listener after successful stop, corrected absent-envelope decoder,
oversized browser snapshot/text-wait limitations, download timeout, CommonJS
variable collision, scoped Git EPERM retry, codec typo and the detected cache.
The owner-XLSX inspector completed decoding but exited1 while printing Arabic
through cp1252; a separate verifier validated its saved decoded outputs. None
of these failed commands is relabelled exit0 or used to excuse a product defect.

Before this single documentation child is committed, npm run check, exact
eight-path scope, verbatim review, matrix prefix, 86-marker comparison, encoding,
whitespace/local links and unchanged-source gates are required. Executed results,
raw commit/tree/source identities and bidirectional full-index patch proof are
supplied externally. Fresh pre-push checks must bind the exact authorized chain,
destination/ref and clean main; only a non-forced exact-SHA fast-forward is
authorized. Final remote/tracking/local equality, runtime/database observations,
cleanup and full preservation comparisons are part of the five-file delivery.

Limits: no screen-reader speech, full accessibility certification, visual Excel
desktop inspection or off-machine recovery guarantee. No further implementation
or operational expansion is authorized after independent-review delivery.
