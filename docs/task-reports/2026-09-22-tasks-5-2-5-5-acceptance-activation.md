# Tasks 5.2–5.5 acceptance and activation

Operational observations: **26 September 2026**. The 22 September filename is the fixed handoff
contract path, not the execution date. The owner accepted the independent implementation PASS and
candidate `3cf48f1252cb173bcb4c24bed477d35a0b4b60a3` in this same local Windows chat and explicitly authorized the bounded
shared recovery/rehearsal, activation, genuine owner sign-in with exact accounting, five-document
child and ordinary non-force publication to existing `khalloda/litigation-system` main. No subagents,
model change, new dependencies, services or purchases were used. Stage 6 is not started.

## Accepted source and baseline

The accepted source parent is `e3236e5d039e3b78d3290fce56a7770d0c8099b5`; its tree is
`bf8ae20c896bef5c3de3f74c0ae6e8923f83b626`. All 859 identities/857 shareable bodies and the documented
nine Windows representations were verified, together with the original ten implementation/R1
delivery identities and the supplied handoff verifier. The full accepted source remains unchanged.
The independent [T5255-R1 PASS](../reviews/2026-09-22-tasks-5-2-5-5-independent-implementation-review.md)
is imported verbatim. README and the [matrix](../testing/tasks-5-2-5-5-acceptance-matrix.md) preserve
their complete accepted prefixes. Only the exact four Tasks 5.2–5.5 checkbox bytes change; all
82 other checkbox lines and the remaining TASKS bytes remain exact.

The fresh baseline at 05:21 UTC had no port3000 listener and an explicitly refused anonymous HTTP
request. The preactivation and failed-launch retry observations confirmed that absence. The old
Task5.1 artifact was retained; historical PID54168 was never used as a current stop target. The
healthy owner database remained in its existing container and cluster. Fresh baseline counts were
141 tables, 48 sequences, 74 ledger rows/73 completed migrations and 885 audit rows. Complete logical
table/column hashes, sequence definitions/states, ownership/grants/default privileges, routines,
triggers, constraints, indexes, role/credential-derived identities, settings, audit/counters,
capabilities, billing/lookups and historical partitions were captured privately read-only.

## Recovery, rehearsal and stable artifact

Protected recovery: `D:/Projects/LitigationData/DB-Backup/migration63/pre-task52-55-activation-20260926T051626Z`. The fresh consistent database dump is
`b0e12405071a4a96d52a7fcfc99084546110b64382d459cfa684276ce8905822` (SHA-256).
Role/configuration/launch material and all 54 registered retained logo versions are inventoried
with exact hashes. The complete owner baseline was unchanged after backup and again before launch.
This is the approved local-development recovery exception; no off-machine or disk-loss protection
is claimed. Production D15/D16 obligations remain unchanged.

That exact dump was restored into one newly owned cluster with distinct credentials, loopback
port, network and storage. Pristine comparison passed before any fixture write. Only the explicitly
enumerated restore-only physical differences were used: cluster/fixture credentials, WAL log-count
reset, dropped-column physical slots, NULL/default ACL representation with effective grants exact,
and the established constraint deparser equivalence. These exceptions never apply to owner equality.
Fresh pristine output records 148 historical and 15 setup checks, all passed. No migration rehearsal,
owner migration, checksum repair or provisioning occurred; all 73 completed migration bindings and
the complete ledger remain exact, including the approved migration0033 terminal-newline case.

Stable artifact: `D:/Projects/LitigationData/accepted-task52-55-3cf48f12-20260926T051626Z`. It was exported from the exact accepted source, used physical
artifact-owned copies of the existing locked dependencies/generated client, and passed a fresh build
and full `npm run check` on the isolated environment. BUILD_ID: `JO3kxiRRVIBhBqcP4LIdL`. It is the
same build used for isolated testing and owner activation. Full inventories preserve all 37,582
pre-existing artifact source/compiled/dependency files. One new artifact-owned cache file and the
two new runtime logs are separately enumerated; no complete-byte-equality claim hides additions.

The four-role production-browser smoke used real Cairo time, genuine fixture passwords and two
clearly marked fixture-only hearings after pristine validation. All six services matched independent
raw SQL/row calculations, including every monthly/annual bucket and unknown/undated coverage,
complete Today/Open lists, all fifth-position ties, person states, refresh, detail/back and entity
links. Three axe scans found no violations; 12 control-focus and three table-focus observations
passed at desktop/narrow widths. The complete post-login read window was equal. All 21 screenshots,
incomplete axe rules, browser outputs and executed helper versions are retained. The exact test app
and disposable database/network/storage were removed with ownership checks; the owner resources
and all prior artifacts were retained.

## Activation and actual owner proof

The accepted app started once at 2026-09-26T06:02:59.584Z, PID `7036`, after the corrected helper's
complete artifact recheck. Its executable/command/artifact, process start, BUILD_ID and sole listener
`127.0.0.1:3000` were positively matched. Login returned HTTP200 and anonymous home access redirected
to login. The app uses unchanged owner restricted runtime credentials/AUTH_SECRET, approved owner
logo root and existing Chromium, real system time, and no privileged migration URL, fixture values
or controlled-clock preload. The new private launch-environment body is hashed but not published.

No supported existing owner session was available. The owner personally signed in through the normal
local form and confirmed it in this chat. No credential was requested in chat, guessed, reset or
copied; no cookie/token was extracted or fabricated. The live dashboard showed Khaled Helmy's
Administrator account. Exact accounting: account 2 changed only `last_login_at` and `updated_at`;
audit event 886 records that login timestamp and event 887 records successful login. Every prior
audit row remained exact, with the audit allocator advancing by two. The accepted account-update
statement trigger also advanced `_migration.staff_roster_mutex.revision` by exactly one; current
minus-one row/column hashes reproduce the private baseline, and live trigger/function definitions
match the accepted schema. No staff/business record, credentials, capabilities, roles or session
version changed. The entire database is therefore **not** claimed unchanged across sign-in.

After reconciliation, a new baseline was established and proved identical to the reconciled state.
Complete equality held through the actual-owner read-only window: 141 tables, 48 sequences and all
catalog/ledger/protected vectors; 887 audit rows. No restore exception masked an owner change.
The fresh actual-owner 148 historical plus 15 setup database gates also passed.

The supported in-app browser independently verified all six panels for Cairo date `2026-09-26`:
Today has hearing6101; Open has 30 exact ordered IDs across two pages; workload has 11 displayed
people, 507 distinct active matters and 39 unassigned; top clients have IDs238,168,245,12,242 with
counts46,39,38,26,21. The 12 monthly and five annual chart/table buckets match the independent owner
oracle and all coverage totals. The five-year interval is 2022-01-01 inclusive to 2027-01-01 exclusive
for the observed 2026 Cairo date, derived dynamically; the current year is labelled incomplete.
There are 13,382 actual owner hearing records, with no fixture additions in the owner database.

Genuine Today/Open detail/back, parent matter3234, staff2 and client238 links, refresh, Arabic font/RTL,
multiline text and 390px reflow passed. One control-focus and three horizontally scrolling table-focus
observations were verified visually; all columns were keyboard reachable. Actual-owner console
errors/warnings were empty. The viewport override was reset and the owner session retained signed in.
The actual-owner session was Administrator only; four-role/axe coverage belongs to the isolated run.

## Preservation, attempts and evidence limits

The complete final preserved-file comparison passed for 344,476 files, 49 junctions and 114 named
root ACLs, with no static byte/size/mtime change, log growth or concurrent addition. New private and
recovery descendant ACLs passed separately. Old root-ACL coverage does not claim every old descendant
ACL was inspected. Private dumps/configurations/passwords and full database/auth captures remain
outside Git/shared delivery. The raw shareable SQL/DOM observations and screenshots are included.

Retained attempts: initial helper copying failed on one known CP1252 dash before private/owner work;
the new helper corrected only that label. An inline quoting attempt and several read-only path/glob
lookups failed without mutation. Scoped CIM/esbuild/private-file moves required supported elevated
execution after sandbox denial. The first launcher rejected the unchanged `localhost` owner URL
before creating environment/log/process state; fresh absence proof preceded the loopback-host helper
correction and successful retry. No source or owner configuration changed. Hash inventory reads
were parallelized without reducing coverage. The first authentication comparison refused the roster
mutex delta; exact accepted-trigger proof resolved it without reverting owner state. The first
post-refresh focus target was BODY; its overbroad diagnostic is retained privately, not counted as
focus proof. A browser evaluation helper replaced unavailable `parseFloat` with `Number`; ordinary
arrow keys brought one initially clipped table outline into view. Original attempts remain retained.
Any subsequent documentation/publication/package attempts are recorded in the external delivery,
without inventing their future results in this commit.

Unchanged implementation proofs are reused by exact source/test/package bindings: 480 permission
decisions, 66 denial cases, literal boundary/year-rollover/edge oracles, full-volume/group/tie cases,
and earlier accessibility/zoom checks. These remain dated implementation evidence, not fresh runs.
Axe incomplete rules and the absence of screen-reader speech testing are explicit; no universal
accessibility claim is made. Offline integrity verification cannot independently read withheld raw
private database or recovery bodies and is distinct from operational review.

Evidence root: `test-results/task52-55-operations-20260926T051626Z`. Its fresh command exits, frozen helper versions, recovery/
source/runtime inventories, authentication proof, actual browser observations, preservation and
cleanup receipts support this checkpoint. Original implementation/R1 deliverables remain exact.

## Documentation child and publication boundary

Only the five contract documentation paths are authorized in the child of the accepted candidate;
856 existing identities remain unchanged and the final tree has 861 identities. The running app
remains bound to the accepted candidate, not a rebuild of the documentation child. Normal non-force
publication of the reviewed chain to existing origin/main is authorized only after the final
documentation gates. This report does not claim that a future push already occurred or embed a
future commit ID. The external final receipt supplies child/parent/tree, actual publication outcome,
clean local/tracking/remote equality and post-seal runtime/database/recovery/cleanup observations.

Independent Tasks 5.2–5.5 operational review remains pending. No Stage6/Task6.1, Ubuntu deployment or
Access cutover is authorized or started by this cycle.
