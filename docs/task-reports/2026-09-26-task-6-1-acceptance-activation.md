# Task 6.1 — acceptance, activation and bounded publication

Operational record prepared 2026-09-26T12:25:33.287846+00:00. Khaled accepted the independent PASS for
`7da48b65a7ba9c1a4d349b970dc7d297489acae4` and authorized this scoped operation
in the same Local Windows chat without subagents. Task 6.2 has not started.

## Accepted source and operational boundary

The ordinary running artifact is exact source `7da48b65…`, the same application
code as implementation `c0e6f53c…`. The new child is documentation only: README
append, one Task 6.1 checkbox, matrix append, verbatim independent review import,
and this report. External receipts identify that child and the actual normal
publication outcome; this document does not embed a prospective self hash.

The production report catalog remains truthfully empty. Engineering definitions
exist only in a separate fixture harness with one recorded composition override
at `src/lib/reports/production.ts`. No owner report/export, sample registration,
hidden activation switch or later report-family work occurred.

Migration and provisioning are **NOT APPLICABLE**. All 73 completed migrations,
74 ledger rows and their checksums are preserved, including migration0033's
documented terminal-newline provenance. No migration, capability grant/revoke,
owner credential change or account provisioning was performed.

## Recovery and preservation

Fresh protected recovery: `D:/Projects/LitigationData/DB-Backup/migration63/pre-task61-activation-20260926T110321Z-61a`. Manifest SHA-256
`2a3e9fdc567d4655ef434b07d8eedac324a9e1fa11c3cdeade853bc19b97e72f`; 54 registered logos retain exact paths,
sizes and bytes. Full owner observations around the read-only backup matched.
The exact dump was restored only to the positively identified disposable cluster;
data/catalog/grant/sequence equality allowed only the enumerated physical restore
differences. Owner comparisons applied no restore exceptions.

The complete baseline/final content-hash cycle covers 437,663
pre-existing files and 68 junctions. No pre-existing static content
changed; any allowed log-prefix growth/concurrent additions are itemized in the
comparison. Named old-root ACLs were checked; this does not claim every old
descendant ACL. New private/recovery directories are checked completely.
After the owner signed in and restarted Codex Desktop to restore its browser
connector, fresh enumeration found the newly accepted app absent. Its cause is
not established. A supplementary matching post-authentication recovery snapshot
was created at `D:/Projects/LitigationData/DB-Backup/migration63/pre-task61-activation-20260926T110321Z-61a-relaunch01` before relaunching the same source/build.
Its full owner equality, complete ACLs and pg_restore archive inspection passed.
The original dump was fully restored/rehearsed; no second full restore of the
supplemental dump is claimed. Both manifests and every member remain bound.
The earlier recovery creation receipt is preserved with its pending status;
subsequent separate equality, restore/gate and ACL evidence establishes completion.

Local recovery does not protect against laptop/disk loss or satisfy later
production off-device backup obligations.

## Fresh rehearsal and source-bound reuse

- Fresh ordinary production build and full project check passed, with exact
  accepted source, owned dependencies, generated files and immutable build hashes.
  `REPORT_ASSET_ROOT` resolves this stable artifact's accepted branding and three
  bundled Noto fonts independently of the editor directory.
- Fresh pristine-fixture **148 historical + 15 setup** gates passed. Fresh owner
  **148 + 15** gates used explicit read-only transactions.
- Four genuine fixture roles reached the ordinary home/Reports flow and empty
  catalog, denied engineering IDs, and completed full database read-window
  equality. Test definitions remained isolated in a separate harness build.
- All four roles have actual saved six-row grouped/multiline XLSX/PDF outputs.
  A separately saved one-row real-data result matches an independent SQL oracle
  for date, ID/order, case, decision and archive state. Five selected XLSX files
  passed independent OOXML type/text/RTL/inert-data checks. Seven selected small
  PDFs passed fonts, headers, extraction and page-bound checks; representative
  actual pages were visually inspected. Registered-logo and fallback renders
  exercise the shared PDF component, not an owner report or engine audit phase.
- 320/390/1280px, genuine browser 200% zoom, keyboard validation/focus/retry and
  selected-ID retention passed. Six targeted axe scans had zero violations;
  incomplete items remain separately recorded. No OS/screen-reader speech or
  universal accessibility-conformance claim is made. Accepted repeated PDF
  headers are raster images; body/footer glyph fonts remain embedded.
- Unchanged implementation proof is reused by exact source/dependency/package
  bindings: 480 permission decisions, detailed revocation/audit-failure/cancel
  boundaries, parser/DST/extrema/Unicode/archive/NULL cases, complete layouts,
  13,382-record XLSX and 973-page/58,372,726-byte PDF. The full-volume PDF remains
  close to the reviewed 64 MiB ceiling. It was not regenerated for this cycle.

The original 94,425,760-byte implementation ZIP SHA-256 is
`234c603b77214e8bcbb4e0b896ebcf8da0fc7bf945a020686b8a2bcbad447db4`.
Its original five delivery files were rehashed and receipt-inclusively verified;
they and earlier evidence remain untouched.

## Retained failures and exact fixture accounting

The first harness build rejected an external dependency junction. A physical copy
of the same locked packages resolved that task-owned path issue without changing
accepted config/source. Two browser waits later failed after server-completed PDF
events 914 and 945. Those PDFs are not claimed as browser-received. Matching
HTTP/filename checks and deliberate keyboard/focus sequencing completed the
remaining role gates; their cause is not asserted beyond the retained evidence.
The successful combined role proof reuses valid completed windows across runs.

All 79 fixture audit additions are reconciled: four initial
password/reset-change pairs, fourteen genuine sign-ins, seven previews, fourteen
paired report/export operations, twelve browser-saved files and two completed but
unsaved PDFs. Account-statement mutex increment22 is independently proved from
unchanged trigger definitions and before/after hashes. All 137 other tables,
48 sequences, catalogs and ledger match. Small component renders add one genuine
sign-in but claim no report-engine/export audit events.

Read-only helper output/summary mistakes and their corrected attempts are retained.
An early runtime observation occurred before the launcher completed and therefore
had no launch record; the subsequent observation after return passed. A supported
browser initialization failure and its eventual disposition are documented in
the owner-browser evidence and failure chronology. Full Desktop restart restored
supported browser access; the genuine existing session was reused. Initial
browser click actions did not produce observed navigation, while native Tab/Return
completed both directions. No product-code cause is inferred. The read-only
cleanup helper found a historical test PID reused by a newly created Codex
renderer; process identity/start time and absent test ports prove it is unrelated.
It was preserved. Recovery/helper status, ACL privilege and PowerShell timestamp
failures and their safe scoped resolutions are retained. No gate was weakened. The matrix has a 7,522-byte preserved Windows working
prefix and an identical 7,460-byte canonical Git prefix; both representations
are verified separately, with no historical byte rewrite.

## Actual activation and owner windows

Before switching, successful enumeration found no existing port3000 app. Historical
Stage5 PID7036 was not a stop target and no old app was restarted. The new ordinary
artifact is `D:/Projects/LitigationData/accepted-task61-7da48b65-20260926T110321Z-61a`, build `lkSQrZJNlFp6OuRNKJmmv`. Dated observation
2026-09-26T12:21:32.209Z positively bound PID35464, executable/path/start time, sole
`127.0.0.1:3000` listener, anonymous login200 and protected-home redirect.
It uses restricted runtime credentials, preserved owner AUTH_SECRET/logo root,
controlled installed Chromium, and no migration URL. These are dated health
observations, not a claim of continuous uptime or production service installation.

Actual owner checks used a legitimate session and stayed read-only: home/Reports,
truthful empty catalog, unknown engineering path unavailable, navigation/back,
Arabic/RTL bundled fonts, keyboard focus and responsive behavior; no browser errors.
No owner report or export was executed.

Authentication accounting: one genuine successful owner sign-in; 1 account
changed and 2 exact authentication/row-audit additions.
At 11:58:33 UTC, account 2 changed only `last_login_at` and `updated_at`.
Audit 890 (`record_updated`) and 891 (`login_succeeded`) moved the allocator
from 889 to 891; the unchanged statement trigger moved the staff mutex 16 to 17.
The owner remains 141 tables, 48 sequences, 74 ledger rows and 73 completed
migrations. The post-authentication read window contains 891 audit rows, with
all 889 earlier audit rows preserved. Credentials, roles, capabilities, session versions and all business
data were preserved. Full owner equality applies to the established subsequent
read-only window, not across any successful sign-in. The later receipt refreshes
owner equality, resource absence and app health after sealing.

The exact disposable container/volume/network, test app processes, fixture
credentials and closed browser profiles were removed after preserving proof.
The accepted ordinary app, recovery material, full private comparisons and all
prior artifacts remain. Scope/ACL/resource limits are explicit in the evidence.

## Review delivery and stop

Only the reviewed chain `bcf5359b… → c0e6f53c… → 7da48b65… → documentation child`
is authorized for ordinary non-force publication to existing origin/main. Fresh
remote equality is required before push; final receipts record all four refs,
cleanliness and zero divergence. This is not an Ubuntu deployment or public
network activation. The five-file offline delivery reconstructs accepted/final
trees, exact forward/reverse patch, one checkbox/85 preserved lines, prefixes,
verbatim review, proof bindings and acyclic post-seal receipt.

Evidence: [operational run](../../test-results/task61-acceptance-20260926T110321Z-61a/CONTINUE.md),
[combined browser](../../test-results/task61-acceptance-20260926T110321Z-61a/browser-combined.json),
[independent small outputs](../../test-results/task61-acceptance-20260926T110321Z-61a/small-output-inspection/inspection.json),
[fixture accounting](../../test-results/task61-acceptance-20260926T110321Z-61a/fixture-reconciliation.json),
[owner accounting](../../test-results/task61-acceptance-20260926T110321Z-61a/owner-auth-reconciliation.json),
[preservation](../../test-results/task61-acceptance-20260926T110321Z-61a/files-final-comparison.json),
[failure chronology](../../test-results/task61-acceptance-20260926T110321Z-61a/failures.md).

**Stop for independent Task 6.1 operational review. Task 6.2 has not started.**
