# Task 4.4 Phase 3 — owner acceptance and local activation

15 September 2026. Accepted source: `2966570c368ef484b4d32c77d5bd4c971c19ff4f`.

Khaled Helmy accepted Phase 3 and the [independent PASS review](../reviews/2026-09-15-task-4-4-phase-3-independent-review.md).
Exact reviewed migration 69 is verified on the actual local development database.
The accepted app runs at `http://127.0.0.1:3000`, build `LyN77RMhXZbOvG44OcUhg`.
Overall Task 4.4 remains unchecked. Stop for independent combined review.

## Authority, source and scope

The direct owner message adopted the combined acceptance/activation/publication
mandate in full. Work stayed in this same Local Windows chat and
`D:\Projects\litigation-system`, using the previously authorized current model;
no selector change or subagent is claimed. The new mandate supersedes the dated
candidate-only prohibitions for these named operations. No later task, Ubuntu
deployment, Access cutover or actual business mutation test is included.
D1–D63 and governance remain byte-identical. D59/C1 is **Owner-accepted risk —
unchanged; not technically remediated**. Credentials were not rotated or reset.

All 29 handoff members matched the external manifest. Independently reopened
all 5,692 candidate, 68 Phase 3 reviewer and 23 historical-reviewer archive members:
safe unique names, CRC, byte lengths and SHA-256 matched their complete manifests.
The imported PASS review is verbatim: 17,955 bytes, SHA-256
`5ceb00224521ca16668ae056945179d8c2563f09542f1edfd892be5eb9b62fa0`.
Candidate tree `ce80f945e139e421f9261849d387ecdb1e2ffcbc` has sole parent
`f05787471812c8fb2bd03d31bc44614dad81a8d2`. Fresh narrow origin/main fetch confirmed
the expected published prefix and effective existing HTTPS destination.

## Stable accepted build

All 666 committed files were exported byte-for-byte into
`D:\Projects\LitigationData\accepted-task44-phase3-2966570-20260915T093422Z`. Installed Prisma generation and Next production webpack build
passed with private generated Prisma and .next output. The source manifest and
463 generated/build file identities bind this build. Existing installed dependencies
are shared through a junction, without install or upgrade. Declared package metadata
is separately bound and preserved; this is not a complete transitive dependency
content audit. Build verification checked every source byte afterward. The
documentation child leaves every runtime/build input unchanged, so no relabel rebuild.

## Fresh recovery and isolated rehearsal

Fresh owner identity: localhost:5433/litigation, container litigation-db,
PostgreSQL 17.11, cluster `7676117521894273062`; 68 completed migrations,
one retained historical rollback, zero unfinished, 123 tables and 48 sequences.
The unchanged forced-read-only historical profile passed 135 checks and 15 setup checks.
The exact old app PID 84980, creation 2026-09-14T19:11:11.4874220Z, executable,
command/artifact/build and listener were freshly verified. After zero in-flight
database transactions, only this old app was stopped. Its artifact remains intact.

Recovery root: `D:\Projects\LitigationData\DB-Backup\migration63\pre-migration69-2026-09-15T09-46-40-760Z-5c075150-759f-407e-ba4b-716937cf038a`.
The binary-safe custom dump is 21,864,852 bytes, SHA-256
`ed85eaafd7b8c1c13662723eec3a08df7ac4e005877a8290d3d4c848c0587d2d`. Manifest SHA-256 `92d80647f141105946706292dd96e00d099952c2c9813f2e3d84e150813e72f6`.
All 60 payload members were rehashed, including 54 logos, private role
recovery SQL, unchanged private configuration, named ownership and source/runtime
recovery identities. Existing owner/SYSTEM/Administrators-only ACLs were checked
without modification. This owner-selected local-only backup does not cover laptop
or disk loss. Earlier backups and evidence are retained.

SHARE locks protected the coherent source during backup/rehearsal. The exact dump
was listed and restored with exit-on-error into a distinct owned PostgreSQL 17.11 cluster,
separate loopback port, volume/network and temporary credentials. All original
123 tables/projections, ledger, accounts, history/receipts, named effective rights,
roles and all portable sequence values/is_called matched. 45 log_cnt values reset
to zero on restore and are listed individually; redundant explicit schema-owner
ACLs restore as equivalent implicit defaults. The existing three reviewed
constraint-reparsing equivalents use the unchanged portable comparator. Object
IDs and generated cluster credentials are not claimed equal across clusters.

Before any fixture mutation the restore passed 135 invariants and 15 setup checks. Exact migration 69 deployed through
the unchanged approved direct-migration-principal wrapper. Old projections,
complete fixture sequences, private credentials/account state and the exact named
delta passed, followed by 137 invariants and 15 setup checks. Focused authenticated browser proof used this
same accepted artifact against that isolated restore. Four archive/restore
transitions, independent step state, four confirmation/Cancel/focus flows,
current/archived/all lists, both digit forms, editor/Cancel loads and Arabic RTL at 320px
passed. Fixture sessions used a fresh isolated secret and source-valid claims;
no owner cookie or password was read or changed. Post-browser 137 invariants and 15 setup checks passed.
The owned server/browser/cluster/volume/network were removed after proof.

## Attempts and maintenance interval

The first isolated rehearsal passed 135/137 invariants, 15 setup checks and its
exact migration delta. Its later browser helper selected work ID 4 and incorrectly
expected it on the first page of the intentionally broad substring search for
"4". This was a test selection error. The helper now selects a full-length eligible
ID and records visible result IDs before asserting the selected row. Accepted
application/SQL bytes did not change. The first attempt's source bindings, failure,
completed proof and cleanup remain preserved.

The resume reused the same recovery only after complete fresh owner state,
private credentials/account state, sequences, logos and every backup-member hash
matched. It repeated pristine restore, 135/137/15 checks and completed the focused
browser proof before the single actual deployment. The earlier fixture wrapper's
"no dump file created" cleanup sentence describes that wrapper's internal behavior;
the explicitly authorized protected recovery dump is retained.

The old app stopped at 09:45:57 UTC; the accepted app started at 10:06:26 UTC,
approximately 20 minutes 30 seconds of local maintenance, including that retry.
An anonymous-smoke helper initially had a parse error before any request; its
corrected run passed. A runtime-observation helper initially mishandled an empty
error log; the corrected observation confirmed zero errors. A documentation
helper's LF/CRLF assertion stopped before completing its edits; valid changes
were retained and the bounded edit was completed. These were helper corrections,
not additional actual migration attempts or application changes.

## Actual migration and preservation

Only reviewed `20260915120000_admin_work_archive_restore` was pending.
Its exact 33,800-byte SHA-256 is
`ed6243278987a1d40238584d3bcc61754daa99a6b243e4d7fc77f3b5386b4593`.
All earlier ledger checksums matched frozen source, including the accepted
migration 33 terminal-newline equivalence. Source/build, backup membership,
container identity, full owner state and logos were rechecked immediately before
actual DDL. After zero competing sessions, SHARE protection was released and
the unchanged deploy wrapper ran once, with command-scoped five-second lock timeout.
Actual verification completed `2026-09-15T10:05:54.802Z` before reopening app access.

- All 123 old-table/old-column projections, IDs, business/provenance/ordering,
  row versions, old editing history/receipts and audit rows remain exact.
- Only two default-false non-null archive flags, one immutable boundary singleton
  with exact fresh per-work versions and prior history/receipt digests/counts,
  the reviewed named functions/constraints/indexes/triggers/ACL changes,
  two audit field registrations and the migration 69 ledger entry changed.
- The excluded boundary, flags, audit registrations and ledger row were checked
  separately. Full named schema delta equals rehearsal. Boundary/ledger timestamps
  differ between rehearsal and actual and are recorded; no old data was normalized.
- The actual 123→124 table change added no sequence. All 48 complete last_value/log_cnt/is_called
  vectors and sequence metadata remain exact. Account/password/session and role
  verifier equality was checked privately; no credential-specific fingerprint
  is included in the shareable evidence.
- The actual 137 historical invariants and 15 setup checks passed. There was no reset,
  migration resolve, force override, ledger repair, downgrade, grant workaround
  or restore over actual development.

Fresh unchanged populations: 13,382 hearings; 1,744 matters; 3,694 works; 3,483 steps.
These are dated current totals; older extraction figures remain historical.

## Runtime, browser scope and evidence reuse

PID 66108, created at 2026-09-15T10:06:26.9601830Z, is the verified detached
installed-Node process. It serves the stable artifact at 127.0.0.1:3000
using only the existing restricted runtime/authentication configuration. No
migration-owner credential is passed to the web process. Its exact PID, creation,
command, listener, build/source and database checkpoint are in the runtime receipt.
The launcher survives the invoking shell; no service or scheduled task was added.

Actual GET/HEAD and browser navigation passed Arabic/RTL login, bundled static
assets/fonts, protected administrative/lifecycle and earlier-area route refusals,
protected-logo 401 and 320px login reflow. Streamed HTTP 200 redirects were checked by
redirect content and actual rendered navigation. No business or login form was
submitted, and no actual cookie was injected. Post-smoke full state and protected
file checks passed in the bounded window: all 6,988 inventoried existing files
were rehashed, including configuration, old accepted artifact, 54 logos, prior
backup root, original Phase 3 evidence and verified handoff staging. The separate
receipt states this scope; it is not an inventory of every unrelated historical directory.

**Actual authenticated observation remains pending.** Both advertised interactive
browser mechanisms failed initialization with a missing kernel-assets path;
reset/retry did not resolve it. This does not prove that the owner lacks a valid
session. The mandate permits completion with this explicit gap and exact-build
authenticated isolated proof. Any later actual authenticated supplement must
preserve this original report/commit/package. All actual new archive flags are
false, so no actual archive was manufactured to expose a restore screen.

The independently accepted exhaustive D63 backend/concurrency/corruption,
four-role, D62/ID and canonical controls are reused through the verified original
candidate package/source identities. They are not claimed as new full executions.
The original failed attempts, source-binding limits, static-check encoding
closure and unrerun full user-management suite remain as reviewed. No operating
system screen-reader or full accessibility-conformance claim is made.

## Documentation, publication and delivery

Exactly eight approved paths change: README, TASKS, PRD, DATABASE, MIGRATION,
the append-only Phase 3 matrix, verbatim PASS review and this report. The original
8,948-byte matrix is an exact prefix. All 86 checkbox lines and the 660 other parent
files remain exact; final inventory 668 files. Original implementation reports,
SQL comments, prior evidence, decisions and governance are untouched.

One local child uses subject `docs: accept and activate Task 4.4 Phase 3 locally`,
sole parent 2966570. Its full SHA/tree, patch and verification belong in external
receipts. **This report does not assert a future push succeeded.** After exact
commit verification the authorized operation is an ordinary fixed-full-SHA push
to `https://github.com/khalloda/litigation-system.git`, refs/heads/main. The separate
publication receipt records fresh target observations, actual attempts and final
remote/local equality. No second publication-recording commit, tags, PR or force.

Evidence root: `D:\Projects\LitigationData\review-evidence\task44-phase3-acceptance-activation-publication-20260915\attempt-20260915T093422Z`. It contains sanitized recovery/restore/rehearsal,
actual delta and 137/135/15 logs, full 48 sequence vectors, source/build/runtime and
bounded browser receipts/screenshots, executed helper bindings, exact patch,
pre/postimages/raw Git objects/full inventories and separate operational receipts.
Complete archive verification reopens every safe unique member for CRC/length/hash
and verifies raw Git/source plus forward/reverse patch reconstruction. A separate
delivery receipt binds the final ZIP, manifest, verification and operational files.
Raw dumps/logos/private configuration/role SQL/account fingerprints/session tokens
are excluded. Preserve the running accepted app, old artifact, fresh recovery and
all earlier evidence; stop here for independent review with overall Task 4.4 unchecked.
