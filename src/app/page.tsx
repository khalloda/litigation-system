/*
 * Placeholder home page — task 0.1.
 *
 * Its only job is to prove the application builds and serves. Task 0.4
 * replaces it with the real Arabic right-to-left page, and from that point
 * on no visible string is written inside a component: they all live in
 * src/strings.ts (docs/BRAND.md, decision D12).
 */

export default function Home() {
  return (
    <main>
      <h1>Litigation Management System</h1>
      <p>Skeleton only — task 0.1. No screens, no database, no data yet.</p>
    </main>
  );
}
